<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="88dp"
    android:layout_height="88dp"
    android:background="@drawable/cure_corner"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@mipmap/part_4"
        android:contentDescription="@string/default_title"
        android:id="@+id/cure_item_icon"
        android:scaleType="center"
        />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:text="@string/default_title"
        android:textSize="@dimen/text_view_size"
        android:textAlignment="center"
        android:id="@+id/cure_item_name"
        android:textColor="@color/white"
        />
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/white"
        app:layout_constraintBottom_toBottomOf="@id/cure_item_name"
        android:layout_marginTop="4dp"
        android:id="@+id/cure_item_line"
        android:layout_marginStart="4dp"
        android:layout_marginEnd="4dp"
        />

</android.support.constraint.ConstraintLayout>